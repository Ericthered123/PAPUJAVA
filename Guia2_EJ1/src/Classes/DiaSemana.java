package Classes;

public  enum DiaSemana {
    LUNES,MARTES,MIERCOLES,JUEVES,VIERNES;
}

// Se hace uso de un enum, que es un tipo especial de clase, la cual define un conjunto fijo de constantes
// Los enum son usados para representar valores que no cambian, como los puntos cardinales
// Estos permiten manejar datos constantes de manera mas eficiente y flexible, ya que  un enum
// es una clase completa a la que se le puede agregar mas funcionalidades.